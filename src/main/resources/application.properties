spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
```

## File: `src/main/resources/data.sql`
```sql
-- src/main/resources/data.sql
INSERT INTO customer (name, email) VALUES ('John Doe', 'john@example.com');
INSERT INTO case_record (description) VALUES ('Test Case');
INSERT INTO note (content) VALUES ('Initial note');
```

This modernized Spring Boot application structure addresses dependency management, entity mapping, repository patterns, RESTful API design, and configuration management. It includes basic CRUD operations, validation, and an in-memory database for testing. Each file follows modern best practices for a production-ready application.